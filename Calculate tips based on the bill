const bill = Number(prompt('What is the final bill?'))
const tip = bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.2;
console.log(`the final bill was ${bill} tip should be ${tip}, total sum to be payed is ${bill + tip}`)
